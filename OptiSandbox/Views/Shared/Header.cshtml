@using OptiSandbox.Business.Helpers
@model OptiSandbox.Models.IPageViewModel<OptiSandbox.Models.SitePageData>

@{
    HelperResult ItemTemplate(HtmlHelpers.MenuItem item)
    {
        @Html.PageLink(item.Page)

        return new HelperResult(w => Task.CompletedTask);
    }
}

<div class="header">
    <h1>OptiSandbox</h1>
</div>
<div class="menu">
    <div class="menu-content">
        @Html.MenuList(SiteDefinition.Current.StartPage, ItemTemplate, true)
    </div>
</div>